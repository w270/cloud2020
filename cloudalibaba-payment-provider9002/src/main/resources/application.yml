server:
  port: 9002
spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        #配置nacos地址
        #server-addr: localhost:8848
        server-addr: 192.168.2.201:1111
management:
  endpoints:
    web:
      exposure:
        include: '*'

